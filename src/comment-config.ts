// ==============================================================
// コメント・スタンプの見た目設定
// ここの値を変えて npm run dev（または npm run build）すると反映される。
// ==============================================================
export const COMMENT_CONFIG = {
  // コメントの文字色（この中からランダムに選ばれる）
  colors: [
    "#ffffff",
    "#ffeb3b",
    "#ff5252",
    "#69f0ae",
    "#40c4ff",
    "#ff80ab",
    "#b388ff",
  ],

  // 文字サイズ(px)（この中からランダムに選ばれる）
  fontSizes: [24, 32, 40, 56],

  // 文字の太さ（400=普通, 700=太字, 900=極太）
  fontWeight: 700,

  // 縁取りの色
  outlineColor: "#000000",

  // 縁取りの太さ(px)（0にすると縁取りなし）
  outlineWidth: 1,

  // コメントが画面を流れきる秒数の範囲（小さいほど速い）
  flowSeconds: { min: 5, max: 10 },

  // スタンプの表示サイズ(px)
  stampSize: 96,

  // スタンプが表示されてから消えるまでの時間(ミリ秒)の範囲
  stampMs: { min: 1800, max: 2600 },

  // お祝いモード: この単語がコメントに含まれていたら画面全体に紙吹雪が舞う
  celebrateWords: ["おめでとう", "🎉", "祝"],

  // 紙吹雪の枚数
  confettiCount: 90,

  // 拍手演出: このコードのスタンプが来たら画面下から拍手が大量に湧き上がる
  clapCodes: ["clap"],

  // 一度に湧き上がる拍手の数
  clapBurstCount: 30,
} as const;
